<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_move_line_tree_creatable" model="ir.ui.view">
        <field name="model">account.move.line</field>
        <field name="mode">primary</field>
        <field name="inherit_id" ref="account.view_move_line_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="attributes">
                <attribute name="create">1</attribute>
            </xpath>
        </field>
    </record>
    <record id="action_account_move_line" model="ir.actions.act_window">
        <field name="name">Invoice lines</field>
        <field name="res_model">account.move.line</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="view_move_line_tree_creatable"/>
        <field name="domain">[('move_id', '=', active_id), ('display_type', 'in', ('product', 'line_section', 'line_note'))]</field>
        <field name="context">{'default_move_id': active_id}</field>
        <field name="groups_id" eval="[(4, ref('account.group_account_manager')), (4, ref('account.group_account_invoice')), (4, ref('account.group_account_user'))]"/>
    </record>
</odoo>
