<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="my_access_data">
        <div class="o_portal_my_access_data">
            <h3 class="page-header">Your Access Data</h3>
            <p><strong class="o_portal_my_access_data_user">User:</strong> <span t-field="user_id.login"/></p>
            <p><a href="/my/password" class="btn btn-default btn-xs o_portal_my_access_data_password">Change Password</a></p>
        </div>
    </template>
    <template id="my_password_breadcrumb">
        <li t-if="page_name == 'my_password'">My Password</li>
    </template>
    <template id="my_password">
        <t t-call="portal.portal_layout">
            <t t-set="additional_title">My Password</t>
            <h3>Change Your Password</h3>
            <form action="/my/password" method="post">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                <div class="row o_portal_details">
                    <div class="col-md-8">
                        <div class="row">
                            <div class="col-md-12">
                              <div t-if="error_message" class="alert alert-danger">
                                  <t t-foreach="error_message" t-as="err"><t t-esc="err"/><br /></t>
                              </div>
                            </div>
                            <div t-attf-class="form-group #{error.get('new_password') and 'has-error' or ''} col-lg-6">
                                <label class="control-label" for="new_password">New Password</label>
                                <input type="password" name="new_password" class="form-control" autocomplete="off"/>
                            </div>
                            <div t-attf-class="form-group #{error.get('retype_new_password') and 'has-error' or ''} col-lg-6">
                                <label class="control-label" for="retype_new_password">Retype New Password</label>
                                <input type="password" name="retype_new_password" class="form-control" autocomplete="off"/>
                            </div>
                            <input type="hidden" name="redirect" t-att-value="redirect"/>
                        </div>
                        <div class="clearfix">
                            <p class="alert alert-warning">For security reasons, after changing the password, you must login again.</p>
                            <button type="submit" class="btn btn-primary pull-right mb32 ">
                                Confirm
                                <span class="fa fa-long-arrow-right" />
                            </button>
                        </div>
                    </div>
                </div>
            </form>
        </t>
    </template>
</odoo>
