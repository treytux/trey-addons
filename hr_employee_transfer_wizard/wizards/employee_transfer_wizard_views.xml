<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_employee_transfer_wizard" model="ir.ui.view">
        <field name="name">employee.transfer.wizard.form</field>
        <field name="model">employee.transfer.wizard</field>
        <field name="arch" type="xml">
            <form string="Transfer Employees">
                <group>
                    <field name="employee_ids" widget="many2many_tags"/>
                    <field name="target_company_id"/>
                    <field name="archive_option"/>
                </group>
                <group string="Advanced Options" collapsible="True">
                    <field name="additional_field_ids"
                        widget="many2many_tags"
                        options="{'no_create': True}"
                        help="Select extra employee fields to copy in addition to the safe default fields."/>
                </group>
                <footer>
                    <button name="action_transfer_employees" string="Transfer" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
    <record id="action_employee_transfer_wizard" model="ir.actions.act_window">
        <field name="res_model">employee.transfer.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="hr.model_hr_employee"/>
        <field name="binding_view_types">list,form</field>
        <field name="groups_id" eval="[(4, ref('hr.group_hr_manager'))]"/>
    </record>
</odoo>
