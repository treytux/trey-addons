<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="hr_timesheet_line_search" model="ir.ui.view">
        <field name="model">account.analytic.line</field>
        <field name="inherit_id" ref="hr_timesheet.hr_timesheet_line_search"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="workorder_id"/>
                <field name="production_id"/>
            </field>
            <filter name="groupby_date" position="after">
                <filter string="Production" name="groupby_production" domain="[]" context="{'group_by':'production_id'}"/>
                <filter string="Work Order" name="groupby_workorder" domain="[]" context="{'group_by':'workorder_id'}"/>
            </filter>
        </field>
    </record>
</odoo>
