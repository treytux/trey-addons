<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_mail_retry_send_massive_form" model="ir.ui.view">
        <field name="model">mail.retry.send.massive</field>
        <field name="arch" type="xml">
            <form string="Resend Emails">
                <sheet>
                    <div>
                        <h2>Are you sure you want to resend the selected emails?</h2>
                        <p>This operation cannot be reversed.</p>
                        <small>Can only resend emails in "exception" or "cancel" state.</small>
                    </div>
                </sheet>
                <footer class="o_form_view">
                    <button name="action_retry_send" string="Accept" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
    <record id="mail_retry_send_massive_action" model="ir.actions.act_window">
        <field name="name">Resend Emails</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">mail.retry.send.massive</field>
        <field name="view_type">form</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="mail.model_mail_mail"/>
    </record>
</odoo>
