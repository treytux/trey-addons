<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="academy_training_plan_tree_view" model="ir.ui.view">
        <field name="model">academy.training.plan</field>
        <field name="arch" type="xml">
            <tree string="Training Plans">
                <field name="name"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="user_id"/>
                <field name="typology_id"/>
            </tree>
        </field>
    </record>
    <record id="academy_training_plan_form_view" model="ir.ui.view">
        <field name="model">academy.training.plan</field>
        <field name="arch" type="xml">
            <form string="Training Plans">
                <header>
                    <button name="to_closed" string="Close training plan" type="object"
                        attrs="{'invisible':['|', ('state','=','closed'), ('id','=',False)]}"
                        confirm="This action will close the training plan and all related activities and enrollments."/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Name"/>
                        </h1>
                    </div>
                    <div class="oe_right oe_button_box" name="buttons">
                    </div>
                    <group>
                        <group>
                            <field name="start_date"/>
                            <field name="end_date"/>
                            <field name="company_id" readonly="1"/>
                        </group>
                        <group>
                            <field name="user_id" options="{'no_create': True}"/>
                            <field name="typology_id"/>
                            <field name="analytic_account_id"/>
                            <field name="active" widget="boolean_toggle"/>
                        </group>
                    </group>
                    <notebook>
                        <page name="activities" string="Activities">
                            <field name="activity_ids" readonly='1'>
                                <tree>
                                    <field name="name"/>
                                    <field name="user_id"/>
                                    <field name="teacher_id"/>
                                    <field name="start_date"/>
                                    <field name="end_date"/>
                                    <field name="state" readonly='1'/>
                                </tree>
                            </field>
                        </page>
                        <page name="description" string="Description">
                            <field name="description" nolabel="1"/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers" groups="base.group_user"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>
    <record id="academy_training_plan_search_view" model="ir.ui.view">
        <field name="model">academy.training.plan</field>
        <field name="arch" type="xml">
            <search string="Training Plans">
                <field name="name"/>
                <field name="start_date"/>
                <field name="end_date"/>
            </search>
        </field>
    </record>
    <record id="action_academy_training_plan" model="ir.actions.act_window">
        <field name="name">Training Plans</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">academy.training.plan</field>
        <field name="binding_view_types">form</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="academy_training_plan_search_view"/>
    </record>
</odoo>
