<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="generate_bulletins_wizard" model="ir.ui.view" >
        <field name="model">academy.wizard.generate.bulletin</field>
        <field name="arch" type="xml">
            <form string="Generate bulletins">
                <group>
                    <field name="evaluation_id"/>
                    <field name="date"/>
                </group>
                <footer>
                    <button name="button_accept" class="oe_highlight"
                        type="object" string="Accept"/>
                    <button class="oe_link" special="cancel" string="Cancel"/>
                </footer>
            </form>
        </field>
    </record>
    <record id="action_wizard_generate_bulletins" model="ir.actions.server">
        <field name="name">Generate bulletins</field>
        <field name="model_id" ref="academy.model_academy_activity"/>
        <field name="binding_model_id" ref="academy.model_academy_activity"/>
        <field name="state">code</field>
        <field name="code">
            action = {
                'type': 'ir.actions.act_window',
                'name': 'Generate bulletins',
                'res_model': 'academy.wizard.generate.bulletin',
                'view_mode': 'form',
                'view_type': 'form',
                'target': 'new',
             }
       </field>
    </record>
</odoo>
