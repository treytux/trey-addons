<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_payment_form" model="ir.ui.view">
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">
            <field name="date" position="after">
                <field name="sale_session_id"/>
            </field>
        </field>
    </record>
    <record id="view_account_payment_sale_session_tree" model="ir.ui.view">
        <field name="name">Sale session payments</field>
        <field name="model">account.payment</field>
        <field name="arch" type="xml">
            <tree string="Payments" edit="false">
                <field name="date"/>
                <field name="name"/>
                <field name="journal_id"/>
                <field name="payment_method_id"/>
                <field name="partner_id" string="Customer"/>
                <field name="amount_signed" sum="Amount"/>
                <field name="state"/>
                <field name="company_id" groups="base.group_multi_company"/>
            </tree>
        </field>
    </record>
    <record id="action_account_payment_sale_session" model="ir.actions.act_window">
            <field name="name">Sale session payments</field>
            <field name="res_model">account.payment</field>
            <field name="view_mode">tree,kanban,form,graph</field>
            <field name="domain">[]</field>
            <field name="view_id" ref="sale_session.view_account_payment_sale_session_tree"/>
        </record>
</odoo>
