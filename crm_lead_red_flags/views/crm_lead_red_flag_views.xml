<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="red_flag_act_window" model="ir.actions.act_window">
        <field name="name">Red flags</field>
        <field name="res_model">crm.lead.red.flag</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">Click to create a new red flag.</p>
        </field>
    </record>
    <menuitem
        id="red_flags_menu"
        action="red_flag_act_window"
        parent="crm.menu_crm_config_lead"
        sequence="2"/>
</odoo>
