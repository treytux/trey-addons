<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="report_saleorder_document" inherit_id="sale.report_saleorder_document">
        <xpath expr="//th[@name='th_taxes']" position="before">
            <t t-set="sale_tax_fee_amount" t-value="doc.tax_fee_amount"/>
            <t t-if="sale_tax_fee_amount">
                <th name="th_taxes" class="text-right">Tax fees</th>
            </t>
        </xpath>
        <xpath expr="//td[@name='td_taxes']" position="before">
            <t t-if="sale_tax_fee_amount">
                <td name="td_tax_fees" class="text-right">
                    <span t-esc="''.join(map(lambda x: '(%s: %s)' % (x.tax_fee_id.name, x.tax_fee_amount), line.tax_fee_ids))"/>
                </td>
            </t>
        </xpath>
        <xpath expr="//tr[hasclass('o_subtotal')]" position="before">
            <t t-if="sale_tax_fee_amount">
                <tr id="extra_taxes" class="border-black" style="">
                    <td>Tax fees</td>
                    <td class="text-right">
                        <span t-esc="sale_tax_fee_amount" t-options='{"widget": "monetary", "display_currency": doc.currency_id}'/>
                    </td>
                </tr>
            </t>
        </xpath>
    </template>
</odoo>
